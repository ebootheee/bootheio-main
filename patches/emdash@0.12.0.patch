diff --git a/src/api/handlers/media-allowlist.ts b/src/api/handlers/media-allowlist.ts
index ca5cd4894669f0f4bec62032f036c399bd17379a..419a85daf48d0b38688a6c7beef636fa75a4a8aa 100644
--- a/src/api/handlers/media-allowlist.ts
+++ b/src/api/handlers/media-allowlist.ts	
@@ -13,6 +13,10 @@ export const GLOBAL_UPLOAD_ALLOWLIST: readonly string[] = [
 	"video/",
 	"audio/",
 	"application/pdf",
+	// boothe.io patch: allow 3D model uploads via the block-form media picker
+	// (no fieldId, so per-field widening doesn't apply). Remove when upstream
+	// EmDash exposes a configurable global allowlist or sniffs MIME by extension.
+	"model/",
 ];
 
 /**
diff --git a/src/astro/routes/api/media/upload-url.ts b/src/astro/routes/api/media/upload-url.ts
index da690a3702bdbc7791826a6eb49c8a0e432cbd30..48690b11a5d3df4069f97be8d8f3297deaf95ae4 100644
--- a/src/astro/routes/api/media/upload-url.ts
+++ b/src/astro/routes/api/media/upload-url.ts	
@@ -69,7 +69,41 @@ export const POST: APIRoute = async ({ request, locals }) => {
 				500,
 			);
 		}
-		const body = await parseBody(request, mediaUploadUrlBody(maxSize));
+		// boothe.io patch: always normalize STL/3MF contentType from filename
+		// extension. Browsers report inconsistent MIMEs for these files — Chrome
+		// on Windows sends `application/sla` for .stl, Firefox sends empty for
+		// .3mf, etc. The schema and allowlist both reject those, so rewrite to
+		// the canonical model/stl or model/3mf based on extension alone.
+		// Remove when upstream EmDash adds extension-based MIME normalization.
+		const EXT_TO_MIME: Record<string, string> = {
+			".stl": "model/stl",
+			".3mf": "model/3mf",
+		};
+		let parseRequest: Request = request;
+		try {
+			const rawText = await request.clone().text();
+			const parsed: unknown = JSON.parse(rawText);
+			if (parsed && typeof parsed === "object") {
+				const rawBody = parsed as Record<string, unknown>;
+				const filenameForSniff =
+					typeof rawBody.filename === "string" ? rawBody.filename : "";
+				if (filenameForSniff) {
+					const sniffed = EXT_TO_MIME[path.extname(filenameForSniff).toLowerCase()];
+					if (sniffed) {
+						rawBody.contentType = sniffed;
+						parseRequest = new Request(request.url, {
+							method: request.method,
+							headers: request.headers,
+							body: JSON.stringify(rawBody),
+						});
+					}
+				}
+			}
+		} catch {
+			// JSON malformed — let parseBody return the canonical INVALID_JSON.
+		}
+
+		const body = await parseBody(parseRequest, mediaUploadUrlBody(maxSize));
 		if (isParseError(body)) return body;
 
 		// Validate content type (field-aware widening)
diff --git a/src/astro/routes/api/media.ts b/src/astro/routes/api/media.ts
index 4ed579e4e5940d5066d368f02c88d0ffe490a878..81095196cf4bfe8778e2c8b92909bc1946b0682f 100644
--- a/src/astro/routes/api/media.ts
+++ b/src/astro/routes/api/media.ts	
@@ -116,7 +116,19 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		const fieldAllowlist = fieldId ? await resolveFieldAllowlist(emdash.db, fieldId) : null;
 		const allowlist = fieldAllowlist ?? [...GLOBAL_UPLOAD_ALLOWLIST];
 
-		if (!matchesMimeAllowlist(file.type, allowlist)) {
+		// boothe.io patch: normalize STL/3MF MIME from filename extension.
+		// Browsers report inconsistent MIMEs (`application/sla`, empty string,
+		// etc.) and File.type is read-only, so we maintain an effective MIME
+		// alongside it and use it everywhere file.type was used below. Remove
+		// when upstream EmDash handles this.
+		const EXT_TO_MIME_OVERRIDE: Record<string, string> = {
+			".stl": "model/stl",
+			".3mf": "model/3mf",
+		};
+		const sniffedMime = EXT_TO_MIME_OVERRIDE[path.extname(file.name).toLowerCase()];
+		const effectiveMime = sniffedMime ?? file.type;
+
+		if (!matchesMimeAllowlist(effectiveMime, allowlist)) {
 			return apiError("INVALID_TYPE", "File type not allowed", 400);
 		}
 
@@ -151,7 +163,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		await emdash.storage.upload({
 			key: storageKey,
 			body: buffer,
-			contentType: file.type,
+			contentType: effectiveMime,
 		});
 
 		// Get image dimensions from form data (sent by client)
@@ -182,7 +194,7 @@ export const POST: APIRoute = async ({ request, locals }) => {
 		// Create media record
 		const result = await emdash.handleMediaCreate({
 			filename: file.name,
-			mimeType: normalizeMime(file.type),
+			mimeType: normalizeMime(effectiveMime),
 			size: file.size,
 			width,
 			height,
